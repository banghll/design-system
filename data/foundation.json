{
  "$doc": "전역 토큰. 값이 리터럴로 적히는 유일한 층이다. 컴포넌트는 여기를 참조만 한다.",
  "$generated": "app/globals.css 의 @generated:tokens 구획 — node scripts/gen-tokens.mjs",

  "spacing": {
    "$doc": "밀도의 기준. Tailwind 의 p-4 · gap-6 이 전부 이 값 × n 이다. 하나만 줄이면 화면 전체가 조밀해진다.",
    "base": "0.25rem"
  },

  "radius": {
    "$doc": "모서리의 기준. sm~4xl 이 전부 여기서 계산된다 — 값을 따로 적어 두면 기준을 바꿔도 그 자리만 옛 값을 쓴다.",
    "base": "0.625rem"
  },

  "control": {
    "$doc": "한 줄에 나란히 서는 것들의 기준. 버튼·입력·셀렉트·토글이 이 값을 받으므로 줄이 어긋나지 않는다.",
    "height": "spacing.8",
    "paddingX": "spacing.2.5"
  },

  "text": {
    "$doc": "글자 크기. Tailwind 의 text-* 를 그대로 쓴다 — 이름이 갈라지면 어느 쪽이 정본인지 알 수 없다.",
    "xs": "0.75rem",
    "sm": "0.875rem",
    "base": "1rem",
    "lg": "1.125rem"
  }
}
